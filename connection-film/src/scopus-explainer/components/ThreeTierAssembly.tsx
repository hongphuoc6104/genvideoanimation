import React from 'react';
import { PALETTE } from '../palette';

export const ThreeTierAssembly: React.FC = () => (
  <div style={{ padding: 40 }}>
    <h2 style={{ fontSize: 48, color: PALETTE.EMERALD_LIGHT }}>CẤU TRÚC 3 TẦNG</h2>
  </div>
);
