import React from 'react';
import { PALETTE } from '../palette';

export const BankingCaseStudy: React.FC = () => (
  <div style={{ padding: 40 }}>
    <h2 style={{ fontSize: 48, color: PALETTE.CYAN_GLOW }}>CASE STUDY NGÂN HÀNG SỐ</h2>
  </div>
);
