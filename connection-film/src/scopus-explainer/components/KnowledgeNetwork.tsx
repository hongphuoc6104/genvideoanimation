import React from 'react';
import { PALETTE } from '../palette';

export const KnowledgeNetwork: React.FC = () => (
  <div style={{ padding: 40 }}>
    <h2 style={{ fontSize: 48, color: PALETTE.AMBER_LIGHT }}>MA TRẬN TRI THỨC</h2>
  </div>
);
