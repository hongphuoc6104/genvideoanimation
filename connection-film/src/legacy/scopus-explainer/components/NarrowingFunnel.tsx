import React from 'react';
import { PALETTE } from '../palette';

export const NarrowingFunnel: React.FC = () => (
  <div style={{ padding: 40 }}>
    <h2 style={{ fontSize: 48, color: PALETTE.CYAN_GLOW }}>PHỄU THU HẸP</h2>
  </div>
);
